{
  "name": "jkap-league-tools",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix web-app/frontend",
    "dev": "npm run dev --prefix web-app/frontend",
    "lint": "npm run lint --prefix web-app/frontend",
    "start": "npm run start --prefix web-app/frontend"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
